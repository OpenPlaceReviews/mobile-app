import 'abstract_poi_type.dart';

class POICategory extends AbstractPOIType {
  POICategory(keyName, registry) : super(keyName, registry);

  @override
  String toString() {
    return super.toString();
  }
}
